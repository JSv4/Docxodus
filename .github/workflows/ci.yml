name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore OpenXmlPowerTools.sln

      - name: Build library
        run: dotnet build OpenXmlPowerTools/OpenXmlPowerTools.csproj -c Release --no-restore

      - name: Build CLI tool
        run: dotnet build tools/redline/redline.csproj -c Release --no-restore

      - name: Run tests
        run: dotnet test OpenXmlPowerTools.Tests/OpenXmlPowerTools.Tests.csproj -c Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: OpenXmlPowerTools.Tests/TestResults/*.trx

  build-matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore OpenXmlPowerTools.sln

      - name: Build
        run: dotnet build OpenXmlPowerTools/OpenXmlPowerTools.csproj -c Release --no-restore

      - name: Build CLI tool
        run: dotnet build tools/redline/redline.csproj -c Release --no-restore
